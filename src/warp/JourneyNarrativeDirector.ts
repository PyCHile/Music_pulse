import type{AudioFeatures,MusicalState,WarpState,JourneyStage}from'../types';const c=(v:number)=>Math.max(0,Math.min(1,v)),s=(a:number,b:number,x:number)=>{const t=c((x-a)/(b-a));return t*t*(3-2*t)},m=(a:number,b:number,t:number)=>a+(b-a)*t,g=(x:number,cc:number,w:number)=>Math.exp(-Math.pow((x-cc)/w,2)*.5);export interface JourneyNarrative{stage:JourneyStage;progress:number;heartbeat:number;light:number;galaxyReveal:number;idealized:number;lifeReview:number;boundary:number;returnForce:number;finalFade:number;travelFade:number;turnX:number;turnY:number;}export class JourneyNarrativeDirector{private heartClock=0;private climax=false;private climaxAge=0;private bridge=false;private bridgeAge=0;private ret=false;private returnAge=0;update(dt:number,f:AudioFeatures,ms:MusicalState,p0:number):JourneyNarrative{const p=c(p0||0),climaxSignal=(p>.32&&ms.phase==='CLIMAX'&&f.energy>.38)||(p>.38&&f.climaxProbability>.72&&f.energy>.44)||p>.62;if(!this.climax&&climaxSignal){this.climax=true;this.climaxAge=0}if(this.climax)this.climaxAge+=dt;const bridgeSignal=this.climax&&p>.79&&((ms.phase==='RELEASING'||ms.phase==='RECOVERING')||ms.trend<-.035||f.calmness>.50);if(!this.bridge&&(bridgeSignal||p>.895)){this.bridge=true;this.bridgeAge=0}if(this.bridge)this.bridgeAge+=dt;const hook=this.bridge&&p>.88&&((ms.phase==='BUILDING'||ms.phase==='ACCELERATING'||ms.phase==='CLIMAX')||f.crescendo>.13||f.energy>f.mediumTermEnergy+.055);if(!this.ret&&(hook||p>.965)){this.ret=true;this.returnAge=0}if(this.ret)this.returnAge+=dt;let stage:JourneyStage=this.ret?'RETURN':this.bridge?'BOUNDARY':this.climax&&p>.78?'LIFE_REVIEW':this.climax&&(this.climaxAge>13||p>.69)?'IDEALIZED_COSMOS':this.climax?'LIVING_LIGHT':p>.09?'DARK_TUNNEL':'DETACHMENT';const ascent=s(.03,.60,p),bpm=m(76,49,ascent),period=60/bpm;this.heartClock=(this.heartClock+dt)%period;const hp=this.heartClock/period,env=this.climax?0:(1-s(.08,.59,p))*(1-c(f.climaxProbability*.68)),heartbeat=c((g(hp,.060,.021)+g(hp,.205,.038)*.60)*env);const light=this.climax?s(1.8,18,this.climaxAge)*s(.60,.77,p)*(1-s(.86,.93,p)):0,galaxyReveal=this.climax?c(s(5,30,this.climaxAge)*.52+s(.69,.90,p)*.56):0,idealized=this.climax?s(.67,.76,p)*(1-s(.82,.90,p)):0,lifeReview=this.climax?s(.75,.82,p)*(1-s(.88,.93,p)):0,boundary=this.bridge?s(0,9.5,this.bridgeAge):0,returnForce=this.ret?s(0,2.4,this.returnAge):0,finalFade=this.ret?s(1.6,5.8,this.returnAge):0,travelFade=c(light*.34+idealized*.50+boundary*.72+returnForce*.94),turn=boundary*(1-returnForce)*(1-s(.94,.985,p));return{stage,progress:p,heartbeat,light,galaxyReveal,idealized,lifeReview,boundary,returnForce,finalFade,travelFade,turnX:turn*.18,turnY:-turn*.10}}apply(state:WarpState,n:JourneyNarrative):WarpState{const soft=n.light,ideal=n.idealized,review=n.lifeReview,a=n.boundary,r=n.returnForce,remain=1-n.finalFade;return{...state,speed:Math.max(0,state.speed*m(1,.52,soft)*m(1,.32,ideal)*m(1,.22,review)*m(1,.065,a)*m(1,.02,r)*remain),warpIntensity:Math.max(0,state.warpIntensity*m(1,.46,soft)*m(1,.26,ideal)*m(1,.18,review)*m(1,.09,a)*m(1,.02,r)*remain),streakLength:Math.max(0,state.streakLength*m(1,.34,soft)*m(1,.16,ideal)*m(1,.11,review)*m(1,.05,a)*m(1,.01,r)*remain),starDensity:Math.max(.18,state.starDensity*m(1,.90,soft)*m(1,.94,ideal)*m(1,.96,review)*m(1,.80,a)*m(1,.58,r)),fov:m(state.fov,60,c(soft*.40+ideal*.56+review*.64+a*.82+r)),bloom:Math.max(.035,state.bloom*m(1,.80,soft)*m(1,.84,ideal)*m(1,.88,review)*m(1,.62,a)*m(1,.34,r)),dustDensity:Math.max(.02,state.dustDensity*m(1,.78,soft)*m(1,.68,ideal)*m(1,.56,a)),nebulaPresence:Math.min(1,Math.max(state.nebulaPresence,n.galaxyReveal)),darkness:Math.max(.24,state.darkness-m(0,.38,n.galaxyReveal)-m(0,.04,soft)),shimmer:Math.max(.02,state.shimmer*m(1,.48,soft)*remain),compression:state.compression*m(1,.10,a),heartPulse:n.heartbeat,heartStopped:this.climax,soulProgress:n.progress,livingLight:n.light,galaxyReveal:n.galaxyReveal,idealized:n.idealized,lifeReview:n.lifeReview,boundary:n.boundary,returnForce:n.returnForce,finalFade:n.finalFade,travelFade:n.travelFade,journeyStage:n.stage}}}
