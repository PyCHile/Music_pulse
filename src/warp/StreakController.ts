import type{WarpState}from'../types';export class StreakController{getWorldLength(s:WarpState,d:number){const fade=1-(s.travelFade??0);return Math.max(.025,s.streakLength*(.42+(1-d)*1.25)*(.18+.82*fade));}getAlpha(s:WarpState,d:number,seed:number){const fade=1-(s.travelFade??0);return Math.min(1,(.035+(1-d)*(.24+s.warpIntensity*.52)+seed*.065)*(.12+.88*fade));}}
