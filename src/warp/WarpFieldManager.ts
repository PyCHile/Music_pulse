import * as THREE from'three';import type{AudioFeatures,WarpState}from'../types';import{StarTunnelSystem}from'./StarTunnelSystem';export class WarpFieldManager{readonly group=new THREE.Group();readonly starTunnel:StarTunnelSystem;constructor(maxStars:number){this.starTunnel=new StarTunnelSystem(maxStars);this.group.add(this.starTunnel.group);}update(dt:number,s:WarpState,f:AudioFeatures,vp:{x:number;y:number}){this.starTunnel.update(dt,s,f,vp);}dispose(){this.starTunnel.dispose();}}
