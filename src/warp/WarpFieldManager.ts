import * as THREE from'three';import type{AudioFeatures,WarpState}from'../types';import{StarTunnelSystem}from'./StarTunnelSystem';import{NebulaBackdrop}from'./NebulaBackdrop';import{EtherealFiguresSystem}from'./EtherealFiguresSystem';export class WarpFieldManager{readonly group=new THREE.Group();readonly starTunnel:StarTunnelSystem;readonly nebula:NebulaBackdrop;readonly etherealFigures:EtherealFiguresSystem;constructor(maxStars:number){this.nebula=new NebulaBackdrop();this.starTunnel=new StarTunnelSystem(maxStars);this.etherealFigures=new EtherealFiguresSystem();this.group.add(this.nebula.mesh);this.group.add(this.starTunnel.group);this.group.add(this.etherealFigures.group)}update(dt:number,s:WarpState,f:AudioFeatures,vp:{x:number;y:number}){this.nebula.update(dt,s,f,vp);this.starTunnel.update(dt,s,f,vp);this.etherealFigures.update(dt,s,f,vp)}dispose(){this.nebula.dispose();this.starTunnel.dispose();this.etherealFigures.dispose()}}
