import * as THREE from'three';import type{AudioFeatures,WarpState}from'../types';import{StarTunnelSystem}from'./StarTunnelSystem';import{NebulaBackdrop}from'./NebulaBackdrop';export class WarpFieldManager{readonly group=new THREE.Group();readonly starTunnel:StarTunnelSystem;readonly nebula:NebulaBackdrop;constructor(maxStars:number){this.nebula=new NebulaBackdrop();this.starTunnel=new StarTunnelSystem(maxStars);this.group.add(this.nebula.mesh);this.group.add(this.starTunnel.group);}update(dt:number,s:WarpState,f:AudioFeatures,vp:{x:number;y:number}){this.nebula.update(dt,s,f,vp);this.starTunnel.update(dt,s,f,vp);}dispose(){this.nebula.dispose();this.starTunnel.dispose();}}
