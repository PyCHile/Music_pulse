import * as THREE from'three';import type{AudioFeatures,WarpState}from'../types';import{StarTunnelSystem}from'./StarTunnelSystem';import{NebulaBackdrop}from'./NebulaBackdrop';import{DeepSpaceSectorSystem}from'./DeepSpaceSectorSystem';import{GalacticWispSystem}from'./GalacticWispSystem';import{CelestialFlybySystem}from'./CelestialFlybySystem';import{EtherealFiguresSystem}from'./EtherealFiguresSystem';export class WarpFieldManager{readonly group=new THREE.Group();readonly starTunnel:StarTunnelSystem;readonly nebula:NebulaBackdrop;readonly deepSpace:DeepSpaceSectorSystem;readonly galacticWisps:GalacticWispSystem;readonly flybys:CelestialFlybySystem;readonly etherealFigures:EtherealFiguresSystem;constructor(maxStars:number){this.nebula=new NebulaBackdrop();this.deepSpace=new DeepSpaceSectorSystem();this.galacticWisps=new GalacticWispSystem(maxStars<=1200?28:36);this.starTunnel=new StarTunnelSystem(maxStars);this.flybys=new CelestialFlybySystem();this.etherealFigures=new EtherealFiguresSystem();this.group.add(this.nebula.mesh);this.group.add(this.deepSpace.group);this.group.add(this.galacticWisps.group);this.group.add(this.starTunnel.group);this.group.add(this.flybys.group);this.group.add(this.etherealFigures.group)}update(dt:number,s:WarpState,f:AudioFeatures,vp:{x:number;y:number}){this.deepSpace.update(dt,s);const sector=this.deepSpace.maxPresence||0,ds=this.deepSpace as any;if(sector>.01&&ds.sectors){const zFactor=1-sector*.40,scaleBoost=1+sector*.35;for(const item of ds.sectors){if(!item.group.visible)continue;item.group.position.z=Math.min(-35,item.group.position.z*zFactor);item.group.position.x*=1-sector*.07;item.group.position.y*=1-sector*.04;item.group.scale.multiplyScalar(scaleBoost)}}(s as any).spaceSectorPresence=sector;this.nebula.update(dt,s,f,vp);this.galacticWisps.update(dt,s,f);this.starTunnel.update(dt,s,f,vp);if(sector>.01){const tunnel=this.starTunnel as any,pointRelief=.38+.62*(1-sector*.80),lineRelief=.24+.76*(1-sector*.88);for(let i=0;i<tunnel.activeCount;i++){tunnel.pointAlpha[i]*=pointRelief;tunnel.lineAlpha[i*2]*=lineRelief;tunnel.lineAlpha[i*2+1]*=lineRelief}tunnel.pointGeometry.attributes.aAlpha.needsUpdate=true;tunnel.lineGeometry.attributes.aAlpha.needsUpdate=true;tunnel.lineMaterial.uniforms.uIntensity.value*=.34+.66*(1-sector*.74)}this.flybys.update(dt,s);this.etherealFigures.update(dt,s,f)}dispose(){this.nebula.dispose();this.deepSpace.dispose();this.galacticWisps.dispose();this.starTunnel.dispose();this.flybys.dispose();this.etherealFigures.dispose()}}
