import{Canvas}from'@react-three/fiber';import{useMemo,useRef,useState}from'react';import{AudioEngine}from'./audio/AudioEngine';import type{WarpState}from'./types';import{InterstellarWarpScene}from'./scene/InterstellarWarpScene';import{CameraRig}from'./scene/CameraRig';import{PostProcessing}from'./effects/PostProcessing';const initialState:WarpState={family:'INTERSTELLAR_WARP',speed:10,warpIntensity:.3,starDensity:.68,streakLength:1.2,tunnelDepth:120,fov:64,bloom:.35,dustDensity:.18,nebulaPresence:.03,darkness:.92,curvature:0,shimmer:.1,compression:.1};export default function App(){const audioUrl=useMemo(()=>new URL('../music.mp3',window.location.href).href,[]),audioEngine=useMemo(()=>new AudioEngine(audioUrl),[audioUrl]),stateRef=useRef<WarpState>({...initialState}),[started,setStarted]=useState(false),[paused,setPaused]=useState(false),[error,setError]=useState('');const start=async()=>{try{await audioEngine.play();setStarted(true);setPaused(false);setError('');}catch(e){setError(e instanceof Error?e.message:'No fue posible iniciar el audio');}},toggle=async()=>{if(audioEngine.paused){await audioEngine.play();setPaused(false);}else{audioEngine.pause();setPaused(true);}};return <div style={{position:'fixed',inset:0,background:'#000',overflow:'hidden'}}><Canvas camera={{fov:64,near:.1,far:260}} gl={{antialias:false,powerPreference:'high-performance'}} dpr={[.8,1.8]}><InterstellarWarpScene audioEngine={audioEngine} stateRef={stateRef}/><CameraRig stateRef={stateRef}/><PostProcessing/></Canvas>{!started&&<div style={{position:'absolute',inset:0,display:'grid',placeItems:'center',background:'radial-gradient(circle,rgba(5,15,35,.45),rgba(0,0,0,.96) 60%)',color:'#dbeafe'}}><div style={{textAlign:'center'}}><h1 style={{fontWeight:300,letterSpacing:'.08em'}}>Sueño Interestelar</h1><button data-testid="enter" onClick={start} style={{marginTop:22,padding:'12px 24px',borderRadius:999,border:'1px solid #31527a',background:'#07111f',color:'#dbeafe'}}>ENTRAR</button>{error&&<div style={{marginTop:12,fontSize:12}}>{error}</div>}</div></div>}{started&&<button data-testid="pause" onClick={toggle} style={{position:'absolute',left:'50%',bottom:18,transform:'translateX(-50%)',borderRadius:999,border:'1px solid rgba(130,170,220,.2)',background:'rgba(5,12,25,.45)',color:'#cfe3ff',padding:'9px 14px'}}>{paused?'▶':'Ⅱ'}</button>}</div>;}
