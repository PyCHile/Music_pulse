export class AudioEngine{readonly audio:HTMLAudioElement;context:AudioContext|null=null;analyser:AnalyserNode|null=null;source:MediaElementAudioSourceNode|null=null;frequencyData:Uint8Array<ArrayBuffer>|null=null;timeData:Uint8Array<ArrayBuffer>|null=null;constructor(public readonly url:string){this.audio=new Audio(url);this.audio.preload='auto';this.audio.setAttribute('playsinline','');this.audio.loop=false;}async ensureReady():Promise<void>{if(!this.context){const AudioCtor=window.AudioContext??(window as typeof window&{webkitAudioContext?:typeof AudioContext}).webkitAudioContext;if(!AudioCtor)throw new Error('Web Audio API unavailable');this.context=new AudioCtor({latencyHint:'interactive'});this.analyser=this.context.createAnalyser();this.analyser.fftSize=2048;this.analyser.smoothingTimeConstant=.62;this.analyser.minDecibels=-95;this.analyser.maxDecibels=-18;this.source=this.context.createMediaElementSource(this.audio);this.source.connect(this.analyser);this.analyser.connect(this.context.destination);this.frequencyData=new Uint8Array(this.analyser.frequencyBinCount);this.timeData=new Uint8Array(this.analyser.fftSize);}if(this.context.state==='suspended')await this.context.resume();}async play():Promise<void>{await this.ensureReady();await this.audio.play();}pause():void{this.audio.pause();}get paused():boolean{return this.audio.paused;}get ended():boolean{return this.audio.ended;}get currentTime():number{return this.audio.currentTime||0;}}
