import type{AudioFeatures,MusicalState,MusicalPhase}from'../types';export class MusicalStateAnalyzer{private phase:MusicalPhase='CALM';private timeInPhase=0;private trend=0;update(features:AudioFeatures,dt:number):MusicalState{this.timeInPhase+=dt;const instantTrend=(features.mediumTermEnergy-features.longTermEnergy)*3.5;this.trend+=(instantTrend-this.trend)*(1-Math.exp(-2.2*dt));let next:MusicalPhase=this.phase;if(features.climaxProbability>.70&&features.energy>.56)next='CLIMAX';else if(this.phase==='CLIMAX'&&this.trend<-.03)next='RELEASING';else if(this.trend>.13&&features.energy>.45)next='ACCELERATING';else if(this.trend>.045&&features.energy>.30)next='BUILDING';else if(features.energy<.20)next='CALM';else if(this.trend<-.045)next='RECOVERING';const minHold=this.phase==='CLIMAX'?1.25:.75;if(next!==this.phase&&this.timeInPhase>=minHold){this.phase=next;this.timeInPhase=0;}return{phase:this.phase,intensity:features.energy,trend:this.trend,duration:this.timeInPhase};}}
