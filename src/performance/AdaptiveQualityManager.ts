import type{WebGLRenderer}from'three';import{StarTunnelSystem}from'../warp/StarTunnelSystem';export class AdaptiveQualityManager{private frames=0;private elapsed=0;private quality=1;private cooldown=0;constructor(private renderer:WebGLRenderer,private maxStars:number){this.apply();}private apply(){const mobile=/iPad|iPhone|Android/i.test(navigator.userAgent)||innerWidth<800,base=Math.min(devicePixelRatio||1,mobile?1.5:2);this.renderer.setPixelRatio(Math.max(.8,base*this.quality));}update(dt:number,tunnel:StarTunnelSystem){this.frames++;this.elapsed+=dt;this.cooldown=Math.max(0,this.cooldown-dt);if(this.elapsed<2)return;const fps=this.frames/this.elapsed;this.frames=0;this.elapsed=0;if(this.cooldown>0)return;if(fps<38&&this.quality>.62){this.quality=Math.max(.62,this.quality-.1);this.apply();this.cooldown=4;}else if(fps>56&&this.quality<1){this.quality=Math.min(1,this.quality+.05);this.apply();this.cooldown=5;}tunnel.setActiveCount(Math.floor(this.maxStars*(.56+.44*this.quality)));}}
