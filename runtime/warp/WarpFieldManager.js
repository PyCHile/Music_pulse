import * as THREE from 'three';
import { StarTunnelSystem } from './StarTunnelSystem.js?v=20260812-19';
import { NebulaBackdrop } from './NebulaBackdrop.js?v=20260812-22';
import { DeepSpaceSectorSystem } from './DeepSpaceSectorSystem.js?v=20260812-5';
import { GalacticWispSystem } from './GalacticWispSystem.js?v=20260812-3';
import { CelestialFlybySystem } from './CelestialFlybySystem.js?v=20260812-4';
import { EtherealFiguresSystem } from './EtherealFiguresSystem.js?v=20260812-png7';
const dustVertex=`attribute float aSize;varying float vFade;void main(){vec4 mv=modelViewMatrix*vec4(position,1.0);vFade=clamp(1.0-(-mv.z/140.0),0.0,1.0);gl_PointSize=aSize*clamp(90.0/max(5.0,-mv.z),0.3,2.1);gl_Position=projectionMatrix*mv;}`;
const dustFragment=`varying float vFade;uniform float uOpacity;void main(){float d=length(gl_PointCoord-vec2(.5));float a=smoothstep(.5,.10,d)*uOpacity*(.20+vFade*.80);gl_FragColor=vec4(vec3(.18,.32,.62),a);}`;
export class WarpFieldManager{
 constructor(maxStars){this.group=new THREE.Group();this.nebula=new NebulaBackdrop();this.nebula.geometry.dispose();this.nebula.geometry=new THREE.SphereGeometry(220,96,64);this.nebula.mesh.geometry=this.nebula.geometry;this.deepSpace=new DeepSpaceSectorSystem();this.galacticWisps=new GalacticWispSystem(maxStars<=1200?28:36);this.starTunnel=new StarTunnelSystem(maxStars);this.flybys=new CelestialFlybySystem();this.flybys.group.visible=false;this.etherealFigures=new EtherealFiguresSystem();this.group.add(this.nebula.mesh);this.group.add(this.deepSpace.group);this.group.add(this.galacticWisps.group);this.group.add(this.starTunnel.group);this.group.add(this.flybys.group);this.group.add(this.etherealFigures.group);const dustCount=Math.max(140,Math.floor(maxStars*.075));this.dustPositions=new Float32Array(dustCount*3);this.dustSpeeds=new Float32Array(dustCount);this.dustCount=dustCount;for(let i=0;i<dustCount;i++)this.resetDust(i,true);this.dustGeometry=new THREE.BufferGeometry();this.dustGeometry.setAttribute('position',new THREE.BufferAttribute(this.dustPositions,3));const sizes=new Float32Array(dustCount);for(let i=0;i<dustCount;i++)sizes[i]=.5+Math.random()*.9;this.dustGeometry.setAttribute('aSize',new THREE.BufferAttribute(sizes,1));this.dustMaterial=new THREE.ShaderMaterial({vertexShader:dustVertex,fragmentShader:dustFragment,transparent:true,blending:THREE.AdditiveBlending,depthWrite:false,uniforms:{uOpacity:{value:.034}}});this.dust=new THREE.Points(this.dustGeometry,this.dustMaterial);this.dust.frustumCulled=false;this.group.add(this.dust);}
 resetDust(i,initial=false){const theta=Math.random()*Math.PI*2,r=5+Math.random()*34,p=i*3;this.dustPositions[p]=Math.cos(theta)*r;this.dustPositions[p+1]=Math.sin(theta)*r;this.dustPositions[p+2]=initial?-10-Math.random()*130:-140-Math.random()*20;this.dustSpeeds[i]=.46+Math.random()*.40;}
 update(dt,state,features){this.deepSpace.update(dt,state);const sector=this.deepSpace.maxPresence||0;state.spaceSectorPresence=sector;this.nebula.update(dt,state,features);this.nebula.material.uniforms.uVisibility.value=Math.min(.085,this.nebula.material.uniforms.uVisibility.value*.22);this.galacticWisps.update(dt,state,features);this.starTunnel.update(dt,state,features);if(sector>.01){const pointRelief=.18+.82*(1-sector*.88),lineRelief=.08+.92*(1-sector*.92);for(let i=0;i<this.starTunnel.activeCount;i++){this.starTunnel.pointAlpha[i]*=pointRelief;this.starTunnel.lineAlpha[i*2]*=lineRelief;this.starTunnel.lineAlpha[i*2+1]*=lineRelief;}this.starTunnel.pointGeometry.attributes.aAlpha.needsUpdate=true;this.starTunnel.lineGeometry.attributes.aAlpha.needsUpdate=true;this.starTunnel.lineMaterial.uniforms.uIntensity.value*=.18+.82*(1-sector*.90);}this.flybys.update(dt,state);this.etherealFigures.update(dt,state,features);const fade=1-(state.finalFade||0),light=state.livingLight||0,reveal=state.galaxyReveal||0;this.dustMaterial.uniforms.uOpacity.value=Math.min(.035,(.004+state.dustDensity*.019)*(1-light*.48)*(1-reveal*.28)*(1-sector*.66)*fade);const speed=state.speed*.50;for(let i=0;i<this.dustCount;i++){const p=i*3;this.dustPositions[p+2]+=speed*this.dustSpeeds[i]*dt;if(this.dustPositions[p+2]>-1.5)this.resetDust(i,false);}this.dustGeometry.attributes.position.needsUpdate=true;}
 dispose(){this.nebula.dispose();this.deepSpace.dispose();this.galacticWisps.dispose();this.starTunnel.dispose();this.flybys.dispose();this.etherealFigures.dispose();this.dustGeometry.dispose();this.dustMaterial.dispose();}
}
