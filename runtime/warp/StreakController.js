export class StreakController{getWorldLength(state,depth01){const nearBoost=.42+(1-depth01)*1.25,fade=1-(state.travelFade??0);return Math.max(.025,state.streakLength*nearBoost*(.18+.82*fade));}getAlpha(state,depth01,starSeed){const near=1-depth01,fade=1-(state.travelFade??0);return Math.min(1,(.035+near*(.24+state.warpIntensity*.52)+starSeed*.065)*(.12+.88*fade));}}
