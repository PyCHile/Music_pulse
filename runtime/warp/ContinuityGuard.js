const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
export class ContinuityGuard{constructor(){this.rules=Object.freeze({minForwardMotion:8,requireWarpOpticalFlow:true,requireCentralDepthCue:true,forbidFlatOverlayDominance:true,forbidThemeBreak:true});}enforce(state){return{...state,speed:Math.max(this.rules.minForwardMotion,state.speed),warpIntensity:clamp(state.warpIntensity,.18,1),starDensity:clamp(state.starDensity,.56,1),streakLength:clamp(state.streakLength,.55,8.5),tunnelDepth:clamp(state.tunnelDepth,95,185),fov:clamp(state.fov,60,90),bloom:clamp(state.bloom,.18,1.15),dustDensity:clamp(state.dustDensity,.08,.72),nebulaPresence:clamp(state.nebulaPresence,0,.16),darkness:clamp(state.darkness,.72,.98),curvature:clamp(state.curvature??0,-.05,.05)};}}
