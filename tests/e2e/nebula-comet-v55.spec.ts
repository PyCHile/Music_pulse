import{test,expect}from'@playwright/test';

test('v58 traverses volumetric gas without rectangular billboards and preserves the real-comet system',async({page},testInfo)=>{
 test.setTimeout(120000);const errors:string[]=[];page.on('pageerror',e=>errors.push(e.message));await page.setViewportSize({width:390,height:844});await page.goto('/');await expect(page.locator('#enterBtn')).toBeVisible();await page.waitForFunction(()=>Boolean((window as any).__URUX_BOOT__?.interactionReady),undefined,{timeout:30000});await expect(page.locator('#enterBtn')).toBeEnabled();await page.locator('#enterBtn').click();await page.waitForFunction(()=>Boolean((window as any).__MUSIC_PULSE_QA__?.started)&&Boolean((window as any).__MUSIC_PULSE_QA__?.runtimeReady),undefined,{timeout:30000});
 await page.waitForFunction(()=>{const c=(window as any).__MUSIC_PULSE_QA__?.performanceStats?.scene?.warp?.clouds;return Boolean(c&&c.crossings>0&&c.peakOpacity>.12&&c.rectangularBillboards===false);},undefined,{timeout:45000});
 const runtime=await page.evaluate(()=>{const q=(window as any).__MUSIC_PULSE_QA__,clouds=q.performanceStats?.scene?.warp?.clouds||{},cinematic=q.cinematicEncounterStats||{};return{clouds,realComet:cinematic.realComet||null};});
 expect(runtime.clouds.mode).toBe('continuous-volumetric-traversal');expect(runtime.clouds.volumeType).toBe('3d-point-gas');expect(runtime.clouds.rectangularBillboards).toBe(false);expect(runtime.clouds.supportGeometry).toBe('THREE.Points');expect(runtime.clouds.volumes).toBeGreaterThanOrEqual(3);expect(runtime.clouds.particles).toBeGreaterThanOrEqual(900);expect(runtime.clouds.crossings).toBeGreaterThan(0);expect(runtime.clouds.peakOpacity).toBeGreaterThan(.12);
 if(runtime.realComet){expect(runtime.realComet.redThermalShell).toBe(false);expect(String(runtime.realComet.source)).toContain('67P');}
 const comet=await(await page.request.get('/runtime/journey/CinematicEncounterSystemV55.js')).text();expect(comet).toContain('morfología bilobulada tipo 67P');expect(comet).toContain('overtakePasses');expect(comet).not.toContain('0xff6a18');
 const clouds=await(await page.request.get('/runtime/warp/CloudNebulaVolumeV58.js')).text();expect(clouds).toContain('THREE.Points');expect(clouds).toContain('ShaderMaterial');expect(clouds).toContain("rectangularBillboards:false");expect(clouds).not.toContain('THREE.Sprite');expect(clouds).not.toContain('SpriteMaterial');
 await page.screenshot({path:testInfo.outputPath('v58-volumetric-gas-crossing.png'),animations:'disabled'});expect(errors).toEqual([]);
});
