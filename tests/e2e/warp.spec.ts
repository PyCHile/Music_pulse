import{test,expect}from'@playwright/test';test('source application starts without leaving warp architecture',async({page})=>{const pageErrors:string[]=[];page.on('pageerror',err=>pageErrors.push(err.message));await page.goto('/source/');await expect(page.locator('canvas')).toHaveCount(1);await expect(page.getByTestId('enter')).toBeVisible();await page.getByTestId('enter').click();await page.waitForTimeout(1800);await expect(page.getByTestId('pause')).toBeVisible();expect(pageErrors).toEqual([]);});
