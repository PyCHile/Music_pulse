import{test,expect}from'@playwright/test';
type Snapshot={family:string;speed:number;streakLength:number;fov:number;stage:string;heartbeat:number;livingLight:number;galaxyReveal:number;boundary:number;returnForce:number;finalFade:number;audioStage:string;started:boolean;audioTime:number;duration:number};
async function snap(page:any):Promise<Snapshot>{return page.evaluate(()=>{const qa=(window as any).__MUSIC_PULSE_QA__,s=qa.warpState;return{family:qa.family,speed:s.speed,streakLength:s.streakLength,fov:s.fov,stage:qa.journeyStage,heartbeat:qa.heartbeat,livingLight:qa.livingLight,galaxyReveal:qa.galaxyReveal,boundary:qa.boundary,returnForce:qa.returnForce,finalFade:s.finalFade||0,audioStage:qa.audioStage,started:qa.started,audioTime:qa.audioTime,duration:qa.duration};});}
async function seekFraction(page:any,fraction:number,settle=520){const duration=await page.evaluate(()=>((window as any).__MUSIC_PULSE_QA__?.duration||0));await page.evaluate(({duration,fraction})=>(window as any).__MUSIC_PULSE_QA__.seek(duration*fraction),{duration,fraction});await page.waitForTimeout(settle);return snap(page);}
test('URUX keeps the climax-to-bridge transition gradual before fetal audio takes over',async({page},testInfo)=>{test.setTimeout(90000);const errors:string[]=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('/');await expect(page.locator('#enterBtn')).toBeVisible();await page.locator('#enterBtn').click();await page.waitForFunction(()=>Boolean((window as any).__MUSIC_PULSE_QA__?.started)&&((window as any).__MUSIC_PULSE_QA__?.duration||0)>0,undefined,{timeout:15000});
for(const path of['/assets/ethereal/reference-01.b64','/assets/ethereal/reference-02.b64','/assets/ethereal/reference-03.b64','/assets/ethereal/reference-04.b64']){const response=await page.request.get(path);expect(response.ok()).toBeTruthy();expect((await response.text()).length).toBeGreaterThan(5000);}
const early=await seekFraction(page,.05);expect(early.family).toBe('INTERSTELLAR_WARP');expect(early.stage).toBe('DETACHMENT');let maxHeart=0;for(let i=0;i<8;i++){await page.waitForTimeout(80);maxHeart=Math.max(maxHeart,await page.evaluate(()=>((window as any).__MUSIC_PULSE_QA__?.heartbeat||0)));}expect(maxHeart).toBeGreaterThan(.08);
const tunnel=await seekFraction(page,.25);expect(['DARK_TUNNEL','DETACHMENT']).toContain(tunnel.stage);expect(tunnel.speed).toBeGreaterThan(1);
const preBridge=await seekFraction(page,.60);expect(['DARK_TUNNEL','LIVING_LIGHT']).toContain(preBridge.stage);expect(preBridge.audioStage).not.toBe('FETAL');await page.screenshot({path:testInfo.outputPath('journey-pre-bridge-gradual-light.png'),animations:'disabled'});
const cosmos=await seekFraction(page,.78);expect(['IDEALIZED_COSMOS','LIFE_REVIEW','LIVING_LIGHT']).toContain(cosmos.stage);expect(cosmos.galaxyReveal).toBeGreaterThan(.08);expect(cosmos.audioStage).not.toBe('FETAL');await page.screenshot({path:testInfo.outputPath('journey-cosmos-before-bridge.png'),animations:'disabled'});
const boundary=await seekFraction(page,.91,1500);expect(['BOUNDARY','RETURN']).toContain(boundary.stage);expect(boundary.boundary).toBeGreaterThan(.04);expect(['FETAL','LIGHT']).toContain(boundary.audioStage);
const returning=await seekFraction(page,.975,700);expect(returning.stage).toBe('RETURN');expect(returning.returnForce).toBeGreaterThan(.04);
const ending=await seekFraction(page,.995,2400);expect(ending.stage).toBe('RETURN');expect(ending.finalFade).toBeGreaterThan(.05);expect(errors).toEqual([]);});
